package com.lumibottle.helper;

/**
 * `make few fx in game world? or in game renderer
 *  only change texture/animation.
 */
public class FXHelper {
//when object is killed, create fxobject in that position, wait for the render to render, then delete
}
